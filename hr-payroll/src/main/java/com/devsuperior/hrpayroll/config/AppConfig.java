package com.devsuperior.hrpayroll.config;



//--(IMPORTS)--//

import org.springframework.context.annotation.Configuration;
import org.springframework.web.client.RestTemplate;
import org.springframework.cloud.client.loadbalancer.LoadBalanced;
import org.springframework.context.annotation.Bean;


//--(END)--//


@Configuration
public class AppConfig {

	/*
	//Criando componete apartir de 1 chamada de método
		
	 @Bean		// Isso permite que você injete esse bean em outras partes do seu aplicativo usando @Autowired
	 @LoadBalanced
	 public RestTemplate restTemplate() {
		//return new RestTemplate();
	 }
	   //RestTemplate é uma classe do Spring Framework que facilita a comunicação com serviços web HTTP. 
	   //Ela oferece métodos para enviar requisições e receber respostas
	 */
	
	
	
	//obj de balanceamento de requisições
	
	
	
	
}
