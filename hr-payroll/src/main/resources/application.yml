eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
server:
  port: ${PORT:0} # Altere aqui para o valor padrão desejado
spring:
  application:
    name: hr-payroll
